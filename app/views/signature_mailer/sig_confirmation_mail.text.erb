
<%= t("signature.dear") %> <%= @signature.person_name %> 

<%= t("signature.linkinstruction") %> "<%= @signature.petition.name %>"

<%= url_for(controller: 'signatures',
            action: 'confirm',
            #host: 'localhost:3000',
            host: 'dev.petitions.eu',
            signature_id: @signature.unique_key,
            ) %>

<%= @signature.petition.description %>

<%= t("signature.thanks") %>
