
<%= t("signature.dear") %> <%= @signature.person_name %> 

<%= t("signature.linkinstruction") %>

<%= t("petition.title") %>

<%= @url %>

<%= url_for(controller: 'signatures',
            action: 'confirm',
            #host: 'localhost:3000',
            signature_key: @signature.unique_key,
            ) %>

<%= t("signature.thanks") %>
