<%= t("mail.dear") %> <%= t("mail.status.dear") %> <%= @office.name %>, 

<%= t("mail.status.changed", {
	petition: @petition.name,
	status: @petition.status,
	email: @petition.office.email,
	petitioner: @petition.petitioner_name
}) %>

<%= t("mail.moderation.signoff") %>
