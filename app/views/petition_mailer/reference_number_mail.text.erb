<%= t("mail.dear") %> <%= t("mail.request.dear") %> <%= @office.name %>, 

<%= t("mail.request.reference.",{
	petition: @petition.name,
	date: @petition.date_projected.strftime('%d-%m-%Y'),
	petitioner: @petition.petitioner_name,
	subdomain: @petition.subdomain
}) %>

<%= t("mail.request.speed_norm")%>

<%= t("mail.request.signoff", {
	petitioner: @petition.petitioner_name
}) %>

