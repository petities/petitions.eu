<%= t("mail.dear") %> <%= @user.name %>, 

<%= t("mail.petition.confirm.instructions",{
	petition: @petition.name,
	petitioner: @user.name,
	password: @password,
	confirmation_url: @token
}) %>

<%= confirmation_url(@user, confirmation_token: @token) %>


<%= t("mail.signoff") %> 


